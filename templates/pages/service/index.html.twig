{% extends 'base.html.twig' %}

{% block title %}Heuway -  Service{% endblock %}

{% block body %}
    <div class="container mt-4">
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes('danger') %}
            <div class="alert alert-danger">
                {{ message }}
            </div>
        {% endfor %}
        {% include "partials/_header-services.html.twig" %}
        <h1 class="mt-4">Services</h1>
        <div class="card-columns ">
        {% for service in services %}
            <div class="card border-light mb-3" style="max-width: 20rem;">
                <div class="card-header">Service n°{{ service.id }}</div>
                <div class="card-body">
                    <h4 class="card-title">{{ service.depot }} - {{ service.numerogroupe }}</h4>
                    <p class="card-text">
                    Début : {{ service.debut|date("d/m/Y H:i") }}<br>
                    Fin : {{ service.fin|date("d/m/Y H:i") }}<br>
                    Pause : {{ service.pause|date("H:i") }}
                    </p>
                    <a href="{{ path('service.edit', {id: service.id}) }}"><button type="button" class="btn btn-warning">Modifier</button></a>
                    <a href="{{ path('service.delete', {id: service.id}) }}"><button type="button" class="btn btn-danger">Supprimer</button></a>
                </div>
                
            </div>
        {% endfor %} 
        </div>
        <div class="navigation d-flex justify-content-center">
            {{ knp_pagination_render(services) }}
        </div>
    </div>
{% endblock %}
