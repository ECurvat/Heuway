{% extends "base.html.twig" %}

{% block title %}Heuway - Stats{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col">
            <h1>Stats</h1>
        </div>
    </div>
    {{ form_start(form) }}
    <div class="row">
        <legend>Filtres</legend>
        <div class="col">
            {{ form_row(form.depot) }}
        </div>
        <div class="col">
            {{ form_row(form.ligne) }}
        </div>
        <div class="col">
            {{ form_row(form.debut) }}
        </div>
        <div class="col">
            {{ form_row(form.fin) }}
        </div>
    </div>
    {{ form_end(form) }}
    <div class="row">
        <p>Statistiques effectuées sur {{ services|length }} services</p>
    </div>
</div>
{% endblock %}